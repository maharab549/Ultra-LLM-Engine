numpy>=1.24.0

# Optional -- enables the compiled CUDA fast path in engine/kernels.py.
# Without it, the engine runs correctly on pure NumPy (slower).
# cupy-cuda12x>=12.0.0      # for CUDA 12.x
# cupy-cuda11x>=12.0.0      # for CUDA 11.x

# Optional -- for testing
pytest>=7.0.0

# Checkpoint readers for the supported .safetensors and GGUF paths are
# implemented in engine/loaders/, so no extra checkpoint package is required.
